import React from 'react'

const UserContext=React.createContext()
/* userContext provide all data when we add some data to it then we can access all the data through 
    this provider 
*/

export default UserContext;